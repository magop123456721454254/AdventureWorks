import * as React from 'react';
import Table from '@mui/material/Box';

export default function TableComponent() {
  const [value, setValue] = React.useState('1');

   const handleChange = (event: React.SyntheticEvent) => {
      
    };

    return (
        <Table>
            
        </Table>
    )
}

